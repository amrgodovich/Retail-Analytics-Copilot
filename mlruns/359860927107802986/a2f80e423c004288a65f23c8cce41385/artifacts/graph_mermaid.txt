---
config:
  flowchart:
    curve: linear
---
graph TD;
	__start__([<p>__start__</p>]):::first
	router(router)
	retriever(retriever)
	planner(planner)
	nl2sql(nl2sql)
	execute_sql(execute_sql)
	synth(synth)
	evaluator(evaluator)
	refactor(refactor)
	end(end)
	__end__([<p>__end__</p>]):::last
	__start__ --> router;
	evaluator --> refactor;
	execute_sql --> synth;
	nl2sql --> execute_sql;
	planner --> nl2sql;
	refactor -.-> end;
	refactor -.-> nl2sql;
	refactor -.-> synth;
	retriever -. &nbsp;go_planner&nbsp; .-> planner;
	retriever -. &nbsp;rag_end&nbsp; .-> synth;
	router -. &nbsp;sql&nbsp; .-> planner;
	router -. &nbsp;hybrid&nbsp; .-> retriever;
	synth --> evaluator;
	end --> __end__;
	classDef default fill:#f2f0ff,line-height:1.2
	classDef first fill-opacity:0
	classDef last fill:#bfb6fc
